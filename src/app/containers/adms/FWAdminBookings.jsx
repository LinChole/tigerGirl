import { connect } from "react-redux";
import AdminBookings from "../../components/adms/AdminBookings";


const mapStateToProps = (state) => ({

});

export default connect(mapStateToProps, {

})(AdminBookings);