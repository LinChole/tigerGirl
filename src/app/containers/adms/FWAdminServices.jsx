import { connect } from "react-redux";
import AdminServices from "../../components/adms/AdminServices";


const mapStateToProps = (state) => ({

});

export default connect(mapStateToProps, {

})(AdminServices);